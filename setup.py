from setuptools import setup, find_packages

setup(name="parseq",
      description="parseq",
      author="Sum-Ting Wong",
      author_email="sumting@wo.ng",
      install_requires=[],
      packages=["parseq"],
      )
